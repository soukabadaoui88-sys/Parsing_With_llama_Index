Score = (
    [PDF protégé] → 0 si protégé, sinon OK
    [PDF corrompu] → 0 si corrompu, sinon OK
    (1 - taux_echec) × 30 +
    (confiance_ocr / 100) × 30 +
    (0 si colonnes_détectées sinon 15) +
    (0 si OCR_nécessaire sinon 15) +
    (1 - min(temps_estime / 60, 1)) × 10
)

Seuil de rejet: Score < 60%